{% extends 'admin.html.twig' %}

{% block title %}Modifier service{% endblock %}

{% block body %}
    <section id="edit">
        <h1>Modifier service</h1>
        {{ include('amenities/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        <a class="btn" href="{{ path('amenities_admin') }}">Retour à la liste</a>
        {{ include('amenities/_delete_form.html.twig') }}
    </section>
{% endblock %}
