{% extends 'base.html.twig' %}

{% block title %}Amenities{% endblock %}

{% block body %}
    <h1>Amenities</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ amenity.id }}</td>
            </tr>
            <tr>
                <th>NameAmenity</th>
                <td>{{ amenity.nameAmenity }}</td>
            </tr>
            <tr>
                <th>DescriptionAmenity</th>
                <td>{{ amenity.descriptionAmenity }}</td>
            </tr>
            <tr>
                <th>ImgAmenity</th>
                <td>{{ amenity.imgAmenity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('amenities_index') }}">back to list</a>

    <a href="{{ path('amenities_edit', {'id': amenity.id}) }}">edit</a>

    {{ include('amenities/_delete_form.html.twig') }}
{% endblock %}
