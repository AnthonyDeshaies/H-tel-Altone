{% extends 'base.html.twig' %}

{% block title %}Modifier catégorie de plat{% endblock %}

{% block body %}
    <h1>Modifier catégorie de plat</h1>

    {{ include('category_dishes/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('category_dishes_index') }}">Retour à la liste</a>

    {{ include('category_dishes/_delete_form.html.twig') }}
{% endblock %}
