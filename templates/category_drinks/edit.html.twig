{% extends 'admin.html.twig' %}

{% block title %}Modifier une catégorie de boisson{% endblock %}

{% block body %}
    <section id="edit_category_drink">
        <h1>Modifier une catégorie de boisson</h1>

        {{ include('category_drinks/_form.html.twig') }}
        <section class="links">
            <button class="btn">{{ button_label|default('Enregistrer') }}</button>
            <a class="btn back" href="{{ path('category_drinks_admin') }}">Retour</a>
            {{ include('category_drinks/_delete_form.html.twig') }}
        </section>
    </section>
{% endblock %}
