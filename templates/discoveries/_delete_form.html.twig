<form method="post" action="{{ path('discoveries_delete', {'id': discovery.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ discovery.id) }}">
    <button class="btn">Supprimer</button>
</form>
