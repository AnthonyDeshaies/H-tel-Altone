{% extends 'admin.html.twig' %}

{% block title %}Modifier plat{% endblock %}

{% block body %}
    <section id="edit_dishes" class="container">
        <h1>Modifier ce plat</h1>
        {{ include('dishes/_form.html.twig') }}
        <section class="links">
            <button class="btn">{{ button_label|default('Enregistrer') }}</button>
            <a class="btn back" href="{{ path('dishes_admin') }}">Retour</a>
            {{ include('dishes/_delete_form.html.twig') }}
        </section>
    </section>
{% endblock %}
