{% extends 'admin.html.twig' %}

{% block title %}Modifier plat{% endblock %}

{% block body %}
    <section id="edit" class="container">
        <h1>Modifier ce plat</h1>
        {{ include('dishes/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        <a class="btn" href="{{ path('dishes_admin') }}">Retour à la liste</a>
        {{ include('dishes/_delete_form.html.twig') }}
    </section>
{% endblock %}
