{% extends 'base.html.twig' %}

{% block title %}Modifier plat{% endblock %}

{% block body %}
    <h1>Modifier ce plat</h1>

    {{ include('dishes/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('dishes_index') }}">Retour à la liste des plats</a>

    {{ include('dishes/_delete_form.html.twig') }}
{% endblock %}
