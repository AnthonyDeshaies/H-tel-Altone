{% extends 'base.html.twig' %}

{% block title %}Plats{% endblock %}

{% block body %}
    <h1>plats</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dish.id }}</td>
            </tr>
            <tr>
                <th>Nom du plat</th>
                <td>{{ dish.nameDish }}</td>
            </tr>
            <tr>
                <th>Description du plat</th>
                <td>{{ dish.descriptionDish }}</td>
            </tr>
            <tr>
                <th>Prix du plat</th>
                <td>{{ dish.priceDish }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('dishes_index') }}">Retour à la liste des plats</a>

    <a href="{{ path('dishes_edit', {'id': dish.id}) }}">Modifier ce plat</a>

    {{ include('dishes/_delete_form.html.twig') }}
{% endblock %}
