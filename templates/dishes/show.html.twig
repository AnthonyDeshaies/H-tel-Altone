{% extends 'base.html.twig' %}

{% block title %}Dishes{% endblock %}

{% block body %}
    <h1>Dishes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dish.id }}</td>
            </tr>
            <tr>
                <th>NameDish</th>
                <td>{{ dish.nameDish }}</td>
            </tr>
            <tr>
                <th>DescriptionDish</th>
                <td>{{ dish.descriptionDish }}</td>
            </tr>
            <tr>
                <th>PriceDish</th>
                <td>{{ dish.priceDish }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('dishes_index') }}">back to list</a>

    <a href="{{ path('dishes_edit', {'id': dish.id}) }}">edit</a>

    {{ include('dishes/_delete_form.html.twig') }}
{% endblock %}
