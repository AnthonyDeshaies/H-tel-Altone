{% extends 'admin.html.twig' %}

{% block title %}Modifier boisson{% endblock %}

{% block body %}
    <section id="edit_drink" class="container">
        <h1>Modifier boisson</h1>

        {{ include('drinks/_form.html.twig') }}
        <section class="links">
            <button class="btn">{{ button_label|default('Enregistrer') }}</button>
            <a class="btn back" href="{{ path('drinks_admin') }}">Retour</a>
            {{ include('drinks/_delete_form.html.twig') }}
        </section>
    </section>
{% endblock %}
