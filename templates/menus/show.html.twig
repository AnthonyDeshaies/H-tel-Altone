{% extends 'base.html.twig' %}

{% block title %}Menus{% endblock %}

{% block body %}
    <h1>Menus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ menu.id }}</td>
            </tr>
            <tr>
                <th>Nom du menu</th>
                <td>{{ menu.nameMenu }}</td>
            </tr>
            <tr>
                <th>Prix du menu</th>
                <td>{{ menu.priceMenu }}</td>
            </tr>
            <tr>
                <th>Description du menu</th>
                <td>{{ menu.descriptionMenu |raw }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('menus_index') }}">Retour à la liste des menus</a>

    <a href="{{ path('menus_edit', {'id': menu.id}) }}">modifier menu</a>

    {{ include('menus/_delete_form.html.twig') }}
{% endblock %}
