{% extends 'admin.html.twig' %}

{% block title %}modifier Restaurant{% endblock %}

{% block body %}
    <section id="edit" class="container">
        <h1>Modifier le Restaurant</h1>
        {{ include('restaurant/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        <a class="btn" href="{{ path('restaurant_admin') }}">Retour à la liste</a>
        {{ include('restaurant/_delete_form.html.twig') }}
    </section>
{% endblock %}
