{% extends 'admin.html.twig' %}

{% block title %}Modifier chambre{% endblock %}

{% block body %}
    <section id="edit_rooms">
        <h1>Modifier chambre</h1>

        {{ include('rooms/_form.html.twig') }}
        <section class="links">
            <button class="btn">{{ button_label|default('Enregistrer') }}</button>
            <a class="btn back" href="{{ path('rooms_admin') }}">Retour</a>
            {{ include('rooms/_delete_form.html.twig') }}
        </section>
    </section>
{% endblock %}
