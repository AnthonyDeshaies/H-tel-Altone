{% extends 'admin.html.twig' %}

{% block title %}Modifier fournisseur{% endblock %}

{% block body %}
    <section id="edit_supplier" class="container">
        <h1>Modifier fournisseur</h1>
        {{ include('suppliers/_form.html.twig') }}
        <section class="links">
            <button class="btn">{{ button_label|default('Enregistrer') }}</button>
            <a class="btn back" href="{{ path('suppliers_admin') }}">Retour</a>
            {{ include('suppliers/_delete_form.html.twig') }}
        </section>
    </section>
{% endblock %}
