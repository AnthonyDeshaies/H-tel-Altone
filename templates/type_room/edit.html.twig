{% extends 'admin.html.twig' %}

{% block title %}Modifier le type de chambre{% endblock %}

{% block body %}
    <section id="edit_type_room" class="container">
        <h1>Modifier le type de chambre</h1>

        {{ include('type_room/_form.html.twig') }}
        
        <section class="links">
            <button class="btn">{{ button_label|default('Enregistrer') }}</button>
            <a class="btn back" href="{{ path('type_room_admin') }}">Retour</a>
            {{ include('type_room/_delete_form.html.twig') }}
        </section>
    </section>
{% endblock %}
