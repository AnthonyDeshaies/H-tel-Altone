{% extends 'base.html.twig' %}

{% block title %}TypeRoom{% endblock %}

{% block body %}
    <h1>TypeRoom</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_room.id }}</td>
            </tr>
            <tr>
                <th>NameType</th>
                <td>{{ type_room.nameType }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_room_index') }}">back to list</a>

    <a href="{{ path('type_room_edit', {'id': type_room.id}) }}">edit</a>

    {{ include('type_room/_delete_form.html.twig') }}
{% endblock %}
